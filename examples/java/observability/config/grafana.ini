[server]
protocol = ${GRAFANA_PROTOCOL}
domain = ${GRAFANA_DOMAIN}
http_port = ${GRAFANA_PORT}
root_url = %(protocol)s://%(domain)s:%(http_port)s/

[recording_rules]
default_datasource_uid = PAE45454D0EDB9216
